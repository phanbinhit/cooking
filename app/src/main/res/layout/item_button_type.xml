<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_marginHorizontal="@dimen/dimen_8"
    android:paddingVertical="@dimen/dimen_4"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <Button
        android:id="@+id/btnItemType"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_item_btn_type"
        android:minHeight="@dimen/dimen_32"
        android:maxWidth="@dimen/min_max_width"
        android:minWidth="@dimen/min_max_width"
        android:textAllCaps="false"
        android:textColor="@color/black"
        style="?android:attr/borderlessButtonStyle"
        android:text="Main course"
        android:textSize="@dimen/size_text_big"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>